{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "vercel-build": "npm run build && php -r \"file_exists('.env') || copy('.env.production', '.env');\" && php artisan optimize:clear && php artisan config:cache && php artisan route:cache && php artisan view:cache && php -r \"file_exists('/tmp/database.sqlite') || copy('./database/database.sqlite', '/tmp/database.sqlite');\""
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0.0",
        "vite": "^5.0.0",
        "shx": "^0.3.4"
    }
}
