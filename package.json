{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "vercel-build": "sh vercel-sqlite-setup.sh && vite build --config vite.vercel.config.js && shx mkdir -p dist && shx cp -r public/* dist/"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0.0",
        "vite": "^5.0.0",
        "shx": "^0.3.4"
    }
}
