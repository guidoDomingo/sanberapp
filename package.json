{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "vercel-build": "npm run build && mkdir -p /tmp && cp ./database/database.sqlite /tmp/database.sqlite && chmod 777 /tmp/database.sqlite"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0.0",
        "vite": "^5.0.0",
        "shx": "^0.3.4"
    }
}
